import Creator from "./components/Creator";

export default function App() {
    return (
        <div style={{ padding: 16 }}>
            <Creator id={1} level={0} DeleteChild={() => {}} />
        </div>
    );
}
